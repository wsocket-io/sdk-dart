name: wsocket_io
description: Official Dart SDK for wSocket — realtime pub/sub, presence, history, and push notifications.
version: 0.2.0
homepage: https://github.com/wsocket-io/sdk-dart
repository: https://github.com/wsocket-io/sdk-dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  web_socket_channel: ^2.4.0
  http: ^1.1.0
